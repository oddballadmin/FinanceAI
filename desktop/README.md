# Desktop Shell

Desktop-specific packaging belongs here once the app is ready for it.

The desktop app should consume the Expo web build and keep platform shell code
separate from shared app logic. Do not duplicate feature, data, or UI logic in
this folder.
