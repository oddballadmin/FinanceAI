# Web

FinanceAI's web app is served by Expo Router from the shared `app/` routes.

Use this folder only for web-only deployment notes, static hosting adapters, or
web-only assets that cannot live in the shared Expo app.
